version: '3'

services: 
    ClientsDB:
        image: postgres
        environment: 
          POSTGRES_PASSWORD: "123456"  
          POSTGRES_USER: "AbnnerSales"  
          POSTGRES_DB: "ClientsDB"  
        ports:
          - "5432:5432"
        restart: always
